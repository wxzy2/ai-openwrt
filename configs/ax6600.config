# 雅典娜 AX6600 编译配置
# 替换方法：在本地 openwrt 目录执行：
#   make menuconfig
#   ./scripts/diffconfig.sh > 此文件路径
#
# 以下为示例占位，请替换为你实际导出的 .config 内容
CONFIG_TARGET_ipq807x=y
CONFIG_TARGET_ipq807x_DEVICE_xiaomi_ax6600=y
